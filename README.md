# blockCert

A simple implementation of blockchain in Javascript.

### To Run

`node blockchain.js`

The blockchain mines blocks and adds them to an in-memory array. Then prints out the whole chain and checks the validity of the chain.

There is no peer-to-peer setup yet. That's on the to-do list.
